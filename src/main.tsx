import React from 'react'
import ReactDOM from 'react-dom/client'
import Grid from './components/TicketInsights.js'
import './main.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Grid />
  </React.StrictMode>,
)
